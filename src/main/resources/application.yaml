spring:
  application:
    name: user-service

  datasource:
    url: jdbc:mysql://localhost:3306/user_service_db
    username: root
    password: Kalyan@18

  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true

